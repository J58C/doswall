import 'package:flutter/material.dart';

// --- WARNA UTAMA DAN SEKUNDER ---
const Color primaryColor = Color(0xFF7494EC); // Soft Blue
const Color secondaryColor = Color(0xFF5683FF); // Bold Blue

// --- WARNA LATAR DAN TEKS UNTUK TEMA TERANG ---
const Color lightBackgroundColor = Color(0xFFF4F7FE); // Light Gray
const Color darkTextColor = Color(0xFF212121);      // Dark Gray (untuk teks di atas latar terang)

// --- WARNA AKSEN DAN STATUS ---
const Color accentOrangeColor = Color(0xFFFF9F45);  // Orange
const Color successGreenColor = Color(0xFF4CAF50);  // Green
const Color errorRedColor = Color(0xFFFF4D4F);      // Red

// --- Warna untuk Tema Gelap ---
const Color darkBackgroundColor = Color(0xFF121212);  // Latar Gelap standar Material
const Color darkSurfaceColor = Color(0xFF1E1E1E);   // Surface sedikit lebih terang dari background gelap
const Color lightTextColor = Color(0xFFE0E0E0);     // Teks Terang (untuk teks di atas latar gelap)